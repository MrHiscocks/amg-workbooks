import tkinter as tk

accounts = [
    {"username": "alex", "password": "orbit42"},
    {"username": "sam", "password": "pixel91"}
]

window = tk.Tk()
window.title("Account Login")

# Create authenticate() and the interface.

window.mainloop()