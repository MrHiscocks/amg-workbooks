import tkinter as tk

questions = [
    {"question": "Which keyword defines a function?", "answer": "def"},
    {"question": "Which method gets Entry text?", "answer": "get"}
]
currentQuestion = 0
score = 0

window = tk.Tk()
window.title("Python Quiz")

# Build the interface and command functions.

window.mainloop()