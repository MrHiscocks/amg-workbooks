import tkinter as tk

window = tk.Tk()
window.title("Game Character Creator")
window.geometry("520x420")

# Plan the interface areas and command functions.
# Build and test one feature at a time.

window.mainloop()