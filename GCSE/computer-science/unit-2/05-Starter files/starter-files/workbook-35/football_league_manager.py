import tkinter as tk
from tkinter import ttk, messagebox

teams = []
matches = []

window = tk.Tk()
window.title("Football League Manager")
window.geometry("900x600")

# Decompose into data, calculation, display and event functions.

window.mainloop()