import tkinter as tk
from tkinter import ttk, messagebox

ROOM_FILE = "rooms.txt"
BOOKING_FILE = "bookings.txt"
rooms = []
bookings = []

window = tk.Tk()
window.title("Escape Room Booking")
window.geometry("960x620")

# Create a container and one Frame for each major workflow.
# Keep data functions separate from interface event functions.

window.mainloop()